# No secrets are required anymore — sign-in now uses per-user email/password
# accounts stored in tribune_users.db (created automatically on first run),
# not a single shared APP_PASSWORD. This file is kept only in case you add
# something that needs a secret later (e.g. an external database URL).
