# Copy this file to .streamlit/secrets.toml and set a real password.
# secrets.toml is gitignored — never commit it.
# On Streamlit Community Cloud, set this instead under
# App settings → Secrets (same TOML format).

APP_PASSWORD = "change-me"
